---
name: project-015-fitness-harness
description: feature 015 fitness harness (Gate A) — discharges 014's deferred FR-019; constraint = checks-do-not-bind; GC5 GREEN branch is tautology, TS runtime justified only by GC5 move/restore which is unnecessary
metadata:
  type: project
---

Feature 015 makes 014's three greppable fitness checks (FC1-FC3 in scripts/check-suite-integrity.sh) actually bind on every change. Adds: harness (Vitest/TS — repo's first test runtime), first CI (GitHub Actions), FC4 secret-filter congruence (in shell), GC5 substrate-guard behavioral test (in TS).

**Constraint (Gate A, re-validated 2026-06-21):** Same infra repo, chorus reviewing itself. Binding constraint = suite's own verdict-trust + iteration cost. 015 sharpens it: 014's checks EXIST but do not BIND. "An un-invoked check rots" (plan.md:L32). Wiring them to bind IS the constraint. Harness binding (FR-001/002/003) + GC3/FC4 secret-filter congruence are on-constraint, NOT deferrable (GC3 = FR-010a hard precondition on findings→memory callback; silent divergence = security regression).

**My Gate A findings:**
- G3 (cut): GC5 GREEN branch is a tautology. With no loader enforcing REQUIRED: (FR-008, research R3), GC5 cannot test guard BEHAVIOR — only (a) anchors present in authored SKILL.md, (b) precondition core-reachable true/false. GREEN branch asserts "core present ⇒ precondition true" = checking a dir you didn't move is still there. Motion without learning. RED branch's anchor assertion is the load-bearing half (turns RED on weakened guard text). Collapse GC5 to a grep of authored guard text → FC5 shell stanza, same shape as FC1 residence + FC4.
- G4 (cut/legible-bet): TS runtime (package.json, tsconfig, 2 dev deps, 2 of 3 test files) justified by plan's own Complexity Tracking "for GC5 only" (plan.md:L142). If GC5 is a grep, the runtime's sole justification collapses. Subprocess+exit-code (FR-002) is a 15-line shell/Make wrapper. EITHER drop to shell harness OR keep TS only if team explicitly bets it as FUTURE runtime — and prices it as that, not "GC5 needs it."
- G5: proportionality ceiling (R5: ≤3 files, ≤2 deps) is REAL not theater — but mis-aimed. Real carry cost = new-runtime (npm ci, lockfile, dependabot, Node drift, second language for markdown maintainers to audit), not file count. Aim ceiling at "stays greppable / no new runtime."

**Evaporating cloud the board must rule on:** does GC5 test runtime behavior (needs TS) or authored text (needs grep)? Arm A assumes runtime behavior; Arm B assumes authored text. R3 + FR-008 confirm Arm B (loader doesn't enforce REQUIRED:). Arm A's assumption evaporates. G3+G4 decide together. Does NOT auto-win — Delivery-and-Ops may legitimately want npm/Actions as intended future runtime; that's a forward bet to make legible, not a GC5 requirement.

**Gate A cycle-2 (2026-06-21) — RESOLVED. My G4 🔴 CLEARED; one NEW 🟡.**
ADR-001 discharged G4 honestly and my cycle-1 cut was incomplete. I priced the TS runtime off GC5 ALONE ("GC5 is a grep → runtime collapses"). Correct on GC5; wrong on the aggregate. The operator surfaced the cloud I skipped: the harness's assertion surface is NOT trivial — it must test (a) three-way exit-code mapping 0/1/2 (FR-002), (b) crash-safe move/restore teardown on failure mid-multi-assertion (FR-007), (c) three-way failure vocabulary infra/violation/env each with recovery action (FR-014). Hand-rolling that in bash = building a test framework from scratch (trap EXIT + bespoke reporter + structured RED/GREEN) = MORE code, worse DevEx, and every future check pays the bash-framework tax forever. My Arm A assumption ("assertion surface is trivial") EVAPORATED. The thin pinned runner amortizes the tax — on-constraint (suite's iteration cost), legibly bet in an ADR. G3 cut still correct & honored: GC5 GREEN tautology fixed by truth-in-labeling (FR-006 = guard-text+precondition, not "behavioral"). G5 ceiling correctly re-aimed at "is any check logic in TS?" not file count.

**NEW 🟡 (cycle 2): the re-aimed proportionality ceiling has no gauge.** The runtime is acceptable ONLY because the seam holds (check logic in shell, TS thin); the ONLY enforcer is FC7. FC7 is NAMED (ADR-001:74-75, plan.md:132) but has NO testable definition — no grep pattern, no FR/SC token requiring it to exist+pass. A ceiling measured by "is any check logic in TS?" with no check that measures it is a slogan. Not a 🔴 (bet is sound, seam is right) — but FC7 must get a concrete shell stanza + an SC, or the seam erodes silently and last-round's accepted runtime quietly becomes next-round's all-logic-in-TS. (Same gap, lower severity, on FC6/FR-013: "cue phrasing" the meta-check greps for is never named.) Settling step: FC7 lands as a real shell check (e.g. fail if tests/** contains readFileSync/grep over skill/**, templates/**, *.md canon) with an SC asserting it goes RED on an injected canon-grep in a test file.

**How to apply:** On 015 impl/later gates — (1) harness binding + FC4 congruence + GC3 are hard, on-constraint, never defer; (2) G4 is settled — TS runtime stays, do NOT re-litigate it; it is a legible on-constraint bet, not gold-plating; (3) the live proportionality fight is now FC7/FC6 ENFORCEABILITY — a thin-wrapper ceiling with no gauge is the gold-plating risk now, because it lets the runtime grow unpoliced. Watch impl for FC7 actually shipping as a testable stanza.

Related: [[project-014-suite-decomposition]] (015 discharges 014's deferred FR-019; FC4 extends 014's fitness checks; GC5 makes 014's live-behavioral-proof repeatable).
